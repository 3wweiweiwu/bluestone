const config = require('../../../config')
class Locator {
    /**
     * 
     * @param {*} recordingSteps 
     */
    static async updateLocatorBasedonSteps(locatorPath, recordingSteps, locator) {
        let locatorObj = config.code.locatorPath
        
    }
}
module.exports = Locator