mixin input-arguments(queryKey,operationArguments,queryIndex)
    - queryKey = queryKey
    - operationArguments = operationArguments || []

    each argument, index in operationArguments
        - inputId=queryKey+"_"+index
        label(for=inputId)=argument.text
            div(class="input-group")
                input(type="text" queryIndex=queryIndex queryKey=queryKey index=index class="form-control" id=inputId onChange="redirectInputOnChange(event)" value=argument.value)

    



