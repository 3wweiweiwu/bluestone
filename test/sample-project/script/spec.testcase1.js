let locator = require('../bluestone-locator')
let func = require('../bluestone-func')
describe('test', () => {
    it('should run test1', () => {
        locator.todoPage.todoText.locators
        func.todoPage
    })
})